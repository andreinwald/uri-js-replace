import { URIComponents } from "./index";
export declare function resolve(baseURI: string, relativeURI: string): string;
export declare function resolveComponents(base: URIComponents, relative: URIComponents): URIComponents;
